# Code skill

Read and write files in the workspace.

- Use `read_file` to inspect code.
- Use `write_file` to make changes.
- Use `execute_command` to run builds, tests, linters.

Stay within the workspace directory. Do not modify files outside the workspace.

