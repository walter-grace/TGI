# Git skill

Use git for version control in the workspace.

- Clone repositories when setting up.
- Create branches: `agent/{{issue.identifier}}`
- Commit frequently with descriptive messages.
- Push when ready for review.

