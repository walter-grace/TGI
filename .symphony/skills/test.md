# Test skill

Run tests and validate changes.

- Execute the test suite before considering work complete.
- Fix failing tests before moving to review.
- Use the project's standard test command (e.g. `npm test`).

